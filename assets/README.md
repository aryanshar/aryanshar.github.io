This `assets/` folder holds static files used by the site.

- Put your resume at `assets/resume.pdf` to enable the Resume link.
- Add images under `images/` (e.g. `images/avatar.png`).
This directory holds static assets such as your resume (resume.pdf), images, and other files.

Add `resume.pdf` here to enable the `/resume/` download link.
